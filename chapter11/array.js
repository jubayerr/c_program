// 1 dimensional array in JavaScript

var countryList = ["Bangladesh", "India", "Pakistan", "Sri Lanka", "Nepal", "Bhutan", "Maldives"]

for (let i = 0; i < countryList.length; i++) {
    const element = countryList[i];
    console.log(element);
}